import Reset from "@/components/Reset";

export const metadata = {
    title: "Reset password",
};

const ResetPage = () => {
    return (
        <>
            <Reset />
        </>
    );
};

export default ResetPage;
