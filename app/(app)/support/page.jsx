import Support from "@/components/Support";

export const metadata = {
    title: "Support",
};

const SupportPage = () => {
    return (
        <>
            <Support />
        </>
    );
};

export default SupportPage;
