import Profile from "@/components/Profile";

export const metadata = {
    title: "Your profile",
};

const ProfilePage = () => {
    return (
        <>
            <Profile />
        </>
    );
};

export default ProfilePage;
